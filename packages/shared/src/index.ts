/**
 * @community-hero/shared
 *
 * Shared types, constants, and utility functions used across
 * the web frontend and API backend.
 */

// ── Types ──
export type { ApiResponse, PaginatedResponse, ErrorResponse } from './types.js';

// ── Constants ──
export { ROLES, HTTP_STATUS } from './constants.js';
